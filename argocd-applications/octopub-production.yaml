apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: octopussamples-octopub-production
  annotations:
    argo.octopus.com/project: "octopub"
    argo.octopus.com/environment: "production"
spec:
  project: default
  source:
     repoURL: https://github.com/OctopusDeploy/samples-argo-cd-gitops.git
     targetRevision: HEAD
     path: octopub/manifests/production
  destination:
    server: https://kubernetes.default.svc
    namespace: production
  syncPolicy:
    automated:  
      prune: true
      selfHeal: true
    syncOptions:
    - Validate=false
    - CreateNamespace=true